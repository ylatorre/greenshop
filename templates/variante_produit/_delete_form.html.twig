<form method="post" action="{{ path('app_variante_produit_delete', {'id': variante_produit.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ variante_produit.id) }}">
    <button class="btn">Delete</button>
</form>
