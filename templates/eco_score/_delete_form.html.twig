<form method="post" action="{{ path('app_eco_score_delete', {'id': eco_score.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ eco_score.id) }}">
    <button class="btn">Delete</button>
</form>
